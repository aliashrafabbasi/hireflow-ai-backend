{
  "name": "HireFlow - Resume Email to Slack",
  "nodes": [
    {
      "parameters": {
        "pollTimes": {
          "item": [{ "mode": "everyMinute" }]
        },
        "simple": false,
        "filters": {},
        "options": {
          "downloadAttachments": true,
          "dataPropertyAttachmentsPrefixName": "attachment_"
        }
      },
      "id": "gmail-trigger",
      "name": "Gmail Trigger",
      "type": "n8n-nodes-base.gmailTrigger",
      "typeVersion": 1.2,
      "position": [0, 300],
      "notesInFlow": true,
      "notes": "Select your Gmail OAuth credential. Downloads email attachments."
    },
    {
      "parameters": {
        "jsCode": "function formatEmailFrom(from) {\n  if (!from) return 'Unknown sender';\n  if (typeof from === 'string') return from;\n  if (from.text) return from.text;\n  if (Array.isArray(from.value) && from.value[0]) {\n    const v = from.value[0];\n    if (v.name && v.address) return `${v.name} <${v.address}>`;\n    return v.address || v.name || 'Unknown sender';\n  }\n  return 'Unknown sender';\n}\n\nfunction candidateFromFileName(fileName) {\n  if (!fileName) return 'Unknown Candidate';\n  return fileName\n    .replace(/\\.(pdf|docx?|PDF|DOCX?)$/,'')\n    .replace(/[-_]+/g, ' ')\n    .replace(/\\bresume\\b/gi, '')\n    .replace(/\\bcv\\b/gi, '')\n    .replace(/\\s+/g, ' ')\n    .trim() || 'Unknown Candidate';\n}\n\nconst items = [];\n\nfor (const item of $input.all()) {\n  const binary = item.binary || {};\n  const keys = Object.keys(binary);\n\n  for (const key of keys) {\n    if (!key.startsWith('attachment_')) continue;\n\n    const file = binary[key];\n    const lowerName = (file.fileName || key).toLowerCase();\n    const mime = (file.mimeType || '').toLowerCase();\n\n    const isResume =\n      lowerName.endsWith('.pdf') ||\n      lowerName.endsWith('.docx') ||\n      lowerName.endsWith('.doc') ||\n      mime.includes('pdf') ||\n      mime.includes('word') ||\n      mime.includes('document');\n\n    if (!isResume) continue;\n\n    const fileName = file.fileName || key;\n\n    items.push({\n      json: {\n        emailSubject: item.json.subject || '(no subject)',\n        emailFrom: formatEmailFrom(item.json.from || item.json.From),\n        fileName,\n        candidateName: candidateFromFileName(fileName),\n      },\n      binary: {\n        data: file,\n      },\n    });\n  }\n}\n\nreturn items;"
      },
      "id": "extract-resume",
      "name": "Extract Resume Attachments",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [240, 300]
    },
    {
      "parameters": {
        "jsCode": "// Slow down so you can watch steps in Executions / Editor\nawait new Promise((r) => setTimeout(r, 3500));\nreturn $input.all();"
      },
      "id": "pause-1",
      "name": "⏳ Watch: CV extracted",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [480, 300],
      "notesInFlow": true,
      "notes": "Demo pause (~3.5s) so you can see this step live"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://api:8000/api/v1/auth/login",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "{\n  \"email\": \"YOUR_ADMIN_EMAIL\",\n  \"password\": \"YOUR_PASSWORD\"\n}",
        "options": {}
      },
      "id": "login",
      "name": "Login HireFlow",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [720, 300],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 2000,
      "notesInFlow": true,
      "notes": "IMPORTANT: Replace YOUR_ADMIN_EMAIL and YOUR_PASSWORD"
    },
    {
      "parameters": {
        "jsCode": "await new Promise((r) => setTimeout(r, 3500));\nreturn $input.all();"
      },
      "id": "pause-2",
      "name": "⏳ Watch: logged in",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [960, 300]
    },
    {
      "parameters": {
        "jsCode": "const token = $('Login HireFlow').first().json.access_token;\n\nif (!token) {\n  throw new Error('Login failed — no access_token. Check email/password in Login HireFlow node.');\n}\n\nreturn $('Extract Resume Attachments').all().map((item) => ({\n  json: {\n    ...item.json,\n    access_token: token,\n  },\n  binary: item.binary,\n}));"
      },
      "id": "attach-token",
      "name": "Attach Token + Resume",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1200, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "http://api:8000/api/v1/resumes/upload",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "=Bearer {{ $json.access_token }}"
            }
          ]
        },
        "sendBody": true,
        "contentType": "multipart-form-data",
        "bodyParameters": {
          "parameters": [
            {
              "parameterType": "formBinaryData",
              "name": "file",
              "inputDataFieldName": "data"
            }
          ]
        },
        "options": {}
      },
      "id": "upload-resume",
      "name": "Upload Resume",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1440, 300],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 3000,
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "jsCode": "await new Promise((r) => setTimeout(r, 3500));\nreturn $input.all();"
      },
      "id": "pause-3",
      "name": "⏳ Watch: CV uploaded",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1680, 220]
    },
    {
      "parameters": {
        "jsCode": "const token = $('Attach Token + Resume').first().json.access_token;\nconst resume = $input.first().json;\n\nif (!resume?.id) {\n  throw new Error('Upload failed — no resume id');\n}\n\nreturn [{\n  json: {\n    ...resume,\n    access_token: token,\n    fileName: $('Attach Token + Resume').first().json.fileName,\n    emailFrom: $('Attach Token + Resume').first().json.emailFrom,\n    emailSubject: $('Attach Token + Resume').first().json.emailSubject,\n    candidateName: $('Attach Token + Resume').first().json.candidateName,\n  },\n}];"
      },
      "id": "prepare-match",
      "name": "Prepare Auto Match",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1920, 220]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=http://api:8000/api/v1/match/{{ $json.id }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "=Bearer {{ $json.access_token }}"
            }
          ]
        },
        "options": {}
      },
      "id": "auto-match",
      "name": "Auto Match Best Job",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [2160, 220],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000,
      "onError": "continueErrorOutput",
      "notesInFlow": true,
      "notes": "Auth required. Uses admin threshold for qualified_jobs."
    },
    {
      "parameters": {
        "jsCode": "await new Promise((r) => setTimeout(r, 3500));\nreturn $input.all();"
      },
      "id": "pause-4",
      "name": "⏳ Watch: match done",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [2400, 220]
    },
    {
      "parameters": {
        "jsCode": "const data = $input.first().json;\nconst emailMeta = $('Prepare Auto Match').first().json;\n\nconst candidateName =\n  data.candidate_name ||\n  emailMeta.candidateName ||\n  'Unknown Candidate';\n\nconst best = data.best_job || {};\nconst qualified = Array.isArray(data.qualified_jobs) ? data.qualified_jobs : [];\nconst threshold = data.score_threshold ?? 50;\n\nconst bestMatched = (best.matched_skills || []).slice(0, 10).join(', ') || 'N/A';\nconst bestMissing = (best.missing_skills || []).slice(0, 8).join(', ') || 'None';\n\nconst jobLines = qualified.map((j, idx) => {\n  return `${idx + 1}. *${j.job_title}* @ ${j.company} — *${j.match_score}%*`;\n});\n\nconst text = [\n  `<!channel>`,\n  ``,\n  `👋 *Dear HR Team,*`,\n  ``,\n  `A new candidate resume just landed in HireFlow and has been screened against our open roles.`,\n  ``,\n  `*Candidate:* ${candidateName}`,\n  `*Resume file:* ${data.resume_filename || emailMeta.fileName || 'N/A'}`,\n  `*Received from:* ${emailMeta.emailFrom || 'N/A'}`,\n  `*Email subject:* ${emailMeta.emailSubject || '(no subject)'}`,\n  `*Resume ID:* \\`${data.resume_id || ''}\\``,\n  ``,\n  `*Best matched job*`,\n  `• *${best.job_title || 'N/A'}* @ ${best.company || 'N/A'}`,\n  `  Match score: *${best.match_score ?? 'N/A'}%*`,\n  `  Matched skills: ${bestMatched}`,\n  `  Missing skills: ${bestMissing}`,\n  best.summary ? `  Summary: ${best.summary}` : null,\n  ``,\n  `*Jobs this candidate qualifies for (score ≥ ${threshold}):*`,\n  ...(jobLines.length ? jobLines : ['None']),\n  ``,\n  `_Please review this candidate and proceed with shortlisting if it looks like a fit._`,\n].filter((line) => line !== null).join('\\n');\n\nreturn [{\n  json: {\n    text,\n    candidateName,\n    best_score: best.match_score,\n    qualified_count: qualified.length,\n  },\n}];"
      },
      "id": "format-slack",
      "name": "Format Slack Message",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [2640, 220]
    },
    {
      "parameters": {
        "select": "channel",
        "channelId": {
          "__rl": true,
          "mode": "name",
          "value": "new-channel"
        },
        "text": "={{ $json.text }}",
        "otherOptions": {}
      },
      "id": "slack-notify",
      "name": "Notify HR on Slack",
      "type": "n8n-nodes-base.slack",
      "typeVersion": 2.3,
      "position": [2880, 220],
      "notesInFlow": true,
      "notes": "Slack credential + channel: new-channel"
    },
    {
      "parameters": {
        "jsCode": "function pickError(raw) {\n  if (raw == null) return 'Unknown error';\n  if (typeof raw === 'string') return raw;\n  if (typeof raw === 'number' || typeof raw === 'boolean') return String(raw);\n\n  const detail =\n    raw.detail ||\n    raw.message ||\n    raw.error?.message ||\n    raw.error ||\n    raw.description ||\n    raw.statusMessage ||\n    null;\n\n  if (typeof detail === 'string') return detail;\n  if (detail && typeof detail === 'object') {\n    try { return JSON.stringify(detail); } catch { /* ignore */ }\n  }\n\n  try {\n    return JSON.stringify(raw);\n  } catch {\n    return String(raw);\n  }\n}\n\nconst err = $input.first().json || {};\nconst node =\n  err.node?.name ||\n  err.node ||\n  err.name ||\n  'Upload / Match';\n\nconst msg = pickError(err);\n\nconst text = [\n  `<!channel>`,\n  ``,\n  `🚨 *HireFlow Automation Failed*`,\n  ``,\n  `*Node:* ${node}`,\n  `*Error:* ${msg}`,\n  `*Time:* ${new Date().toISOString()}`,\n  ``,\n  `_Open n8n → Executions → open this run → click failed node._`,\n].join('\\n');\n\nreturn [{ json: { text, node, error: msg } }];"
      },
      "id": "format-error",
      "name": "Format Error Slack",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1920, 480]
    },
    {
      "parameters": {
        "select": "channel",
        "channelId": {
          "__rl": true,
          "mode": "name",
          "value": "new-channel"
        },
        "text": "={{ $json.text }}",
        "otherOptions": {}
      },
      "id": "slack-error",
      "name": "Notify Error on Slack",
      "type": "n8n-nodes-base.slack",
      "typeVersion": 2.3,
      "position": [2160, 480]
    }
  ],
  "connections": {
    "Gmail Trigger": {
      "main": [[{ "node": "Extract Resume Attachments", "type": "main", "index": 0 }]]
    },
    "Extract Resume Attachments": {
      "main": [[{ "node": "⏳ Watch: CV extracted", "type": "main", "index": 0 }]]
    },
    "⏳ Watch: CV extracted": {
      "main": [[{ "node": "Login HireFlow", "type": "main", "index": 0 }]]
    },
    "Login HireFlow": {
      "main": [[{ "node": "⏳ Watch: logged in", "type": "main", "index": 0 }]]
    },
    "⏳ Watch: logged in": {
      "main": [[{ "node": "Attach Token + Resume", "type": "main", "index": 0 }]]
    },
    "Attach Token + Resume": {
      "main": [[{ "node": "Upload Resume", "type": "main", "index": 0 }]]
    },
    "Upload Resume": {
      "main": [
        [{ "node": "⏳ Watch: CV uploaded", "type": "main", "index": 0 }],
        [{ "node": "Format Error Slack", "type": "main", "index": 0 }]
      ]
    },
    "⏳ Watch: CV uploaded": {
      "main": [[{ "node": "Prepare Auto Match", "type": "main", "index": 0 }]]
    },
    "Prepare Auto Match": {
      "main": [[{ "node": "Auto Match Best Job", "type": "main", "index": 0 }]]
    },
    "Auto Match Best Job": {
      "main": [
        [{ "node": "⏳ Watch: match done", "type": "main", "index": 0 }],
        [{ "node": "Format Error Slack", "type": "main", "index": 0 }]
      ]
    },
    "⏳ Watch: match done": {
      "main": [[{ "node": "Format Slack Message", "type": "main", "index": 0 }]]
    },
    "Format Slack Message": {
      "main": [[{ "node": "Notify HR on Slack", "type": "main", "index": 0 }]]
    },
    "Format Error Slack": {
      "main": [[{ "node": "Notify Error on Slack", "type": "main", "index": 0 }]]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "meta": {
    "templateCredsSetupCompleted": false
  }
}
